# Alchemy API Key (必填)
ALCHEMY_API_KEY=your_alchemy_api_key_here

# ==================== Redis 配置 ====================
# Docker Redis 容器 (宿主机直接访问):
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=           # 可选，默认无密码

# 如果后端也在 Docker 内运行 (docker-compose)，改为:
# REDIS_HOST=redis
# REDIS_PORT=6379

# ==================== 服务端口 ====================
WS_PORT=8080
API_PORT=3001

# ==================== 环境 ====================
NODE_ENV=development
