# 服务器配置
PORT=5000
NODE_ENV=development

# Tron API Key (从 https://www.trongrid.io/ 获取)
TRON_API_KEY=your_tron_api_key_here

# Gemini API Key (用于 AI 预测功能)
GEMINI_API_KEY=your_gemini_api_key_here

# ==================== Redis 配置 (Docker 容器) ====================
# 启动 Redis: docker-compose up -d redis
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=           # 可选，默认无密码
